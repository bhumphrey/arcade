# Cloudflare Pages response headers. File names here never change between deploys,
# so keep the browser cache short enough that an update shows up quickly.
/*
  Cache-Control: public, max-age=300, must-revalidate
